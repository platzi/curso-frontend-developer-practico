// FUNCTION SCOPE

function greeting() {
  let userName = 'Male';
  console.log(userName);

  if( userName === 'Male' ) {
    console.log(`Hola ${userName}`);
  }
}

greeting();
console.log(userName);