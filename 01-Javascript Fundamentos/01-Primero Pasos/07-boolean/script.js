let isActive = true
let hasPermisson = false

// Conversión implícita
let result = 5 > 3
console.log(result)

let name = 'Platzi'
console.log(!!name);

// Conversión explícita
let value = 0
let otherValue = -23
let explicitBoolean = Boolean(otherValue)
console.log(explicitBoolean);